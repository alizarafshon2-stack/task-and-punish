name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Setup Android Project
        run: |
          flutter create . --org com.taskandpunish --project-name task_and_punish --platforms=android
          flutter pub get

      - name: Build Debug APK with MinSdk 21
        run: |
          flutter build apk --debug --no-tree-shake-icons --dart-define=flutter.minSdkVersion=21

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: task-and-punish-app
          path: build/app/outputs/flutter-apk/app-debug.apk
